config.js

#*:Zone.Identifier


*.pmtiles
*.geojson
*.geojson.gz
*.pbf
*.fgb
*.json
*.gpkg

*.parquet 

*.csv.zip


# But keep JSON files in the project root (main) folder
!/*.json

# ...und JSON unter public/ (style.json, Sprite) — Vite kopiert public/ 1:1 nach dist/
!/public/**/*.json

# Golden-Reference der Frontend-Tests (muss versioniert sein, sonst fehlt sie der CI)
!/tests/web/__snapshots__/*.json

# Ignore virtual environment
unfallkarte_env/
accident_data/



# !accidents_11-12.pmtiles
# !accidents_12-13.pmtiles
# !combined.pmtiles


# Lokaler Local-first-Symlink auf pipeline/data (nicht committen)
/data

# Node-Dev-Tooling (Vite + Playwright, siehe package.json / vite.config.js)
node_modules/
/dist/
# Lokale Vite-Env (z. B. .env.development.local mit dem lokalen Mapillary-Token)
.env*.local
test-results/
playwright-report/
